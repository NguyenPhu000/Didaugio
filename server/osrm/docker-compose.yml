services:
  osrm:
    image: ghcr.io/project-osrm/osrm-backend:latest
    container_name: osrm-cantho
    ports:
      - "5000:5000"
    volumes:
      - ./data:/data
    command: osrm-routed --algorithm mld --threads 4 /data/cantho.osrm
    restart: unless-stopped
