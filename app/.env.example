# API Server URL
# De app dung duoc khi khac mang/o xa, dat URL public HTTPS tai day.
# Vi du: https://api.yourdomain.com/api hoac https://your-ngrok-domain.ngrok-free.app/api
EXPO_PUBLIC_API_URL=https://api.yourdomain.com/api
EXPO_PUBLIC_API_URL_PROD=https://api.yourdomain.com/api
# Backend port — KHONG phai Metro port!
# Metro chay o PORT/RCT_METRO_PORT (mac dinh 8081, script start dung 8083).
# Chi dung gia tri nay khi USE_RUNTIME_HOST=true hoac co API_FALLBACK_URL
# de sinh ra URL `http://<lan-host>:<EXPO_PUBLIC_LOCAL_API_PORT>/api`.
EXPO_PUBLIC_LOCAL_API_PORT=8081

# URL backup cho may dev noi bo — su dung khi ngrok tunnel chet
# va runtime-host khong kha thi (vd dang dung Expo Go o khoi xa nha).
# Co the trung hoac khac EXPO_PUBLIC_API_URL, nguon fallback cuoi cung.
# Mac dinh ben duoi: LAN IP cua may dev dev dien hinh.
EXPO_PUBLIC_API_FALLBACK_URL=http://192.168.1.13:8081/api

# Chi dung runtime host khi test cung mang LAN voi may chay backend.
# Neu bat true, app se tu thu them http://<may-dev>:<EXPO_PUBLIC_LOCAL_API_PORT>/api.
EXPO_PUBLIC_USE_RUNTIME_HOST=false
# Timeout (ms) cho API tạo lịch trình AI — mặc định 120000 nếu không set
# EXPO_PUBLIC_AI_REQUEST_TIMEOUT=120000
# Required for Expo Go Google OAuth proxy redirect:
# https://auth.expo.io/@<owner>/<slug>
EXPO_PUBLIC_EXPO_OWNER=
EXPO_PUBLIC_EXPO_SLUG=

# Google OAuth Client IDs — Google Cloud Console > Credentials
# Web: BẮT BUỘC khi gọi backend /auth/google để nhận id_token
# Android: Android OAuth client (đúng package + SHA-1)
# iOS: iOS OAuth client; dùng để Expo config plugin tự tạo iosUrlScheme
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=xxxx.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=xxxx.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=
