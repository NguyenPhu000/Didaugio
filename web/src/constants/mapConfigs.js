/**
 * @deprecated — Use `import { MAP_CONFIGS, ... } from "@/modules/map"` instead.
 * This file re-exports for backward compatibility.
 */
export {
  MAP_CONFIGS,
  MAP_STYLES,
  DEFAULT_MAP_STYLE,
  CAN_THO_CENTER,
  MAP_THEME,
  LAYER_IDS,
} from "@/modules/map";
